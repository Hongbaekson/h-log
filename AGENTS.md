# Repository Instructions

이 저장소는 개인 사이트를 작은 단위로 개발하기 위한 모노레포입니다.

## Scope

- 루트 `AGENTS.md`에는 공통 원칙만 둔다.
- 앱 작업 전 가장 가까운 앱의 `AGENTS.md`를 먼저 읽고, 필요한 계획 문서만 추가로 읽는다.
- 저장소 지침이 충돌하면 작업 대상에 더 가까운 `AGENTS.md`를 우선한다.
- 현재 주요 앱은 `apps/h-log`이며, 앱 전용 규칙은 `apps/h-log/AGENTS.md`에 둔다.

## Execution Rules

- 수정 전 현재 파일, 관련 호출 경로, 계획 문서를 확인하고 변경 파일과 성공 기준을 정한다.
- 안전하고 되돌릴 수 있는 모호함은 기본안을 밝히고 진행한다. 범위, 보안, 데이터, 공개 정책, 운영 상태가 달라지는 결정만 사용자에게 확인한다.
- 요청을 해결하는 가장 작은 완결 단위만 구현하고 각 단위마다 가까운 검증을 실행한다.
- 요청과 무관한 리팩터링, 포맷 변경, 파일 이동, 기능 추가는 하지 않는다.
- 기존 변경사항은 사용자 작업으로 보고 되돌리거나 덮어쓰지 않는다.
- 기존 코드와 플랫폼 기능을 먼저 재사용하고, 일회성 추상화나 추측성 설정을 추가하지 않는다.
- 내부 계약상 불가능한 상태를 위한 방어 코드는 추가하지 않는다. 외부 입력, 보안, 개인정보, 데이터 손실 경계의 검증과 오류 처리는 생략하지 않는다.
- 이번 변경으로 불필요해진 코드만 정리한다. 기존 데드 코드는 보고하되 요청 없이 삭제하지 않는다.
- 다단계 작업은 `범위 확인 -> 최소 구현 -> 검증 -> 남은 일 기록` 순서로 진행한다.

## Validation

- 변경에 가장 가까운 테스트를 먼저 실행하고, 앱별 필수 검증은 해당 앱의 `AGENTS.md`를 따른다.
- 문서나 JSON만 변경한 경우 관련 파서 또는 형식 검사와 `git diff --check`를 실행한다.
- UI를 변경한 경우 개발 서버에서 데스크톱과 모바일 뷰포트를 확인한다.
- 완료 전 `git diff`와 `git status`로 범위를 확인한다. 커밋과 푸시는 요청받았을 때만 수행하고 검증한 파일만 스테이징한다.
