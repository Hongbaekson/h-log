# Placeholder-only local PostgreSQL values.
# Production values must live in deploy/postgres.env.production on the server.

POSTGRES_DB=hlog_dev
POSTGRES_USER=hlog_app
POSTGRES_PASSWORD=replace-with-local-dev-password
