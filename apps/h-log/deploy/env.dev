# Placeholder-only local development values.
# Do not put production secrets, server IPs, SSH keys, API keys, or private URLs here.
# Real OCI/CI values must live in server-local files or CI/CD secret storage.

NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1
PORT=3000

DATABASE_URL=postgresql://hlog_app:replace-with-local-dev-password@hlog-postgres:5432/hlog_dev

REDIS_URL=redis://hlog-redis:6379

HLOG_WORKER_MODE=manual
HLOG_PUBLIC_BASE_URL=http://localhost:8080
